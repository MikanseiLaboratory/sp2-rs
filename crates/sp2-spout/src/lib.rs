//! Pure Rust implementation of the Spout2 (2.007) texture sharing protocol.
//!
//! The crate only has content on Windows. On other platforms it is empty so
//! that workspace-wide builds succeed.

#![cfg_attr(not(windows), allow(unused))]
